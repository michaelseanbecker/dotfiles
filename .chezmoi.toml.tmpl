{{- $email := promptString "email" -}}
{{- $location := promptString "location" -}}

[data]
    email = "{{ $email }}"
    location = "{{ $location }}"

[edit]
    command = "code"
    args = ["--wait"]